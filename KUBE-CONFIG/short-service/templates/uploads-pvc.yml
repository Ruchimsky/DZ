apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: uploads-pvc
  namespace: {{ .Values.namespace }}
spec:
  storageClassName: "local-path"
  resources:
    requests:
      storage: 1Gi
  accessModes:
    - ReadWriteOnce